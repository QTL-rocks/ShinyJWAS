# Define UI ----
#rm(list=ls())
# Load required packages
#library(shiny)
#library(shinyFiles)
#library(rclipboard)
#library(ggplot2)
#library(coda)

# Run the app ----
shinyApp(ui = ui, server = server)